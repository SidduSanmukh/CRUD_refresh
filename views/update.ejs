<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="../styles/index.css">
  </head>
    <title>Document</title>
  </head>
  <body>
    
    <div class="w-full h-full bg-zinc-800 p-10 text-white">
        <a href="/read" class="text-1xl text-blue-400 tracking-tighter mb-3">All users</a>
      <h1 class="text-3xl tracking-tighter mb-3">Update user</h1>
         
        <form action="/update/<%= user._id %>" method="POST" class="">
          <input
            type="text"
            class="px-2 py-2 border-1 outline-none border-zinc-600 rounded-lg "
            name="name"
            placeholder="name"
            id="name"
            value="<%=user.name %>"
          />
          <input
            type="text"
            class="px-2 py-2 border-1 outline-none border-zinc-600 rounded-lg "
            name="email"
            id="email"
            placeholder="email"
            value="<%= user.email %>"
          />
          <input
            type="text"
            class="px-2 py-2 border-1 outline-none border-zinc-600 rounded-lg "
            name="image_url"
            id="image"
            placeholder="image url"
            value="<%= user.image_url %>"
          />
          <input type="submit" value="update" class="px-5 py-2 bg-yellow-600 rounded-lg ml-2 outline-none text-black">
        </form>
    </div>
  </body>
</html>
